const express = require('express');

// intitalize the router function
const router = express.Router()






//export the router, remember we are requiring this in the main app.js file
module.exports = router;