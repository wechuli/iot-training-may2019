const names = ["Rose", "Brian", "Jane", "John"];

for (let i = 0; i < names.length; i++) {
  console.log(names[i]);
}


// Loop over the array using for..of


names.push("Jecinta");
console.log(names);


// Add two arrays into one

// Take the previous array and replace the 4th element with 
//names.splice(3,3,Date(),'blah')
const today = new Date();


function addNumbers(){
  return sum
}

addNumbers(1)
addNumbers(0,1,2,3,4)